description: Warp Dark Theme
target: themes/terminals/warp/luma-dark.yaml
variant: dark
content: |
  name: Luma Dark
  accent: "{{color:cyan}}"
  cursor: "{{color:white;luminosity:darker}}"
  background: "{{color:black}}"
  foreground: "{{color:white}}"
  details: darker
  terminal_colors:
    bright:
      black: "{{color:black;luminosity:brighter}}"
      blue: "{{color:blue;luminosity:brighter}}"
      cyan: "{{color:cyan;luminosity:brighter}}"
      green: "{{color:green;luminosity:brighter}}"
      magenta: "{{color:magenta;luminosity:brighter}}"
      red: "{{color:red;luminosity:brighter}}"
      white: "{{color:white;luminosity:brighter}}"
      yellow: "{{color:yellow;luminosity:brighter}}"
    normal:
      black: "{{color:black}}"
      blue: "{{color:blue}}"
      cyan: "{{color:cyan}}"
      green: "{{color:green}}"
      magenta: "{{color:magenta}}"
      red: "{{color:red}}"
      white: "{{color:white}}"
      yellow: "{{color:yellow}}"
