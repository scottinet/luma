description: Warp Light Theme
target: themes/terminals/warp/luma-light.yaml
variant: light
content: |
  name: Luma Light
  accent: "{{color:cyan}}"
  cursor: "{{color:black;luminosity:brighter}}"
  background: "{{color:white}}"
  foreground: "{{color:black}}"
  details: darker
  terminal_colors:
    bright:
      black: "{{color:black;luminosity:brighter}}"
      blue: "{{color:blue;luminosity:brighter}}"
      cyan: "{{color:cyan;luminosity:brighter}}"
      green: "{{color:green;luminosity:brighter}}"
      magenta: "{{color:magenta;luminosity:brighter}}"
      red: "{{color:red;luminosity:brighter}}"
      white: "{{color:white;luminosity:brighter}}"
      yellow: "{{color:yellow;luminosity:brighter}}"
    normal:
      black: "{{color:black}}"
      blue: "{{color:blue}}"
      cyan: "{{color:cyan}}"
      green: "{{color:green}}"
      magenta: "{{color:magenta}}"
      red: "{{color:red}}"
      white: "{{color:white}}"
      yellow: "{{color:yellow}}"
